import { styled } from '@mui/material';

export const Logo = styled('img')(() => ({
  width: '65px',
  height: '32px',
}));

export const BurgerMenuBtn = styled('img')(() => ({
  width: '24px',
  height: '24px',
}));
